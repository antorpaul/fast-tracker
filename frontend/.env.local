# Frontend Environment Variables
# Set NEXT_PUBLIC_API_BASE_URL in Vercel project settings to your backend domain
# Example: https://your-backend.vercel.app
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api

# Optional: if using the /api rewrite in vercel.json, leave unset and fetch("/api/...") will work
